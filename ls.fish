function ls
    command ls -p -G
end

function la
    command ls -A
end

function ll
    command ls -l
end

function lla
    command ls -lA
end

# like windows command prompt
function dir
    command ls -p -G
end